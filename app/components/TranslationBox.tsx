import React from 'react';
import {Text} from 'react-native';

const TranslationBox = ({translatedText}: {translatedText: string}) => {
  return <Text>{translatedText}</Text>;
};

export default TranslationBox;
